package game;

import javax.swing.JButton;

class Button {
    Field field;
    JButton jButton;

    Button(Field field, JButton jButton) {
        this.field = field;
        this.jButton = jButton;
    }
}
